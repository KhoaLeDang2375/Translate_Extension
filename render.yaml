services:
  - type: web
    name: fastapi-backend
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.main:app --host=0.0.0.0 --port=10000
    repo: https://github.com/KhoaLeDang2375/Translate_Extension
    plan: free